import "./Header.css";

export default function Header() {
  return (
    <header className="Top">
      <img src="#" alt="Logo" />

      <nav className="top_nav">
        <a href="#">Home</a>
        <a href="#">Characters </a>
      </nav>
    </header>
  );
}
